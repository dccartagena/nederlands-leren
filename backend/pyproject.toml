[tool.bandit]
# B311: random is used only for shuffling exercises/distractors, never crypto
skips = ["B101", "B104", "B311"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
strict = false

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]
